services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: slacker_mail
    ports:
      - "5432:5432"
    volumes:
      - slacker_mail_pg:/var/lib/postgresql/data

volumes:
  slacker_mail_pg:
